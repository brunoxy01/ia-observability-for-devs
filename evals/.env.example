# Copie para .env nesta pasta (evals/.env já está no .gitignore raiz do projeto)

# Token do Dynatrace gerado por `npm run doctor` (escopo: storage:spans:read,
# storage:events:read, storage:events:write — ver docs/07-evaluations.md)
DT_API_TOKEN=dt0s01.SUBSTITUA_PELO_TOKEN_REAL

# Credencial do judge provider escolhido no dt-eval.yaml — só preencha a que for usar
OPENAI_API_KEY=
ANTHROPIC_API_KEY=
GOOGLE_API_KEY=
AZURE_OPENAI_ENDPOINT=
AZURE_OPENAI_API_VERSION=
AWS_ACCESS_KEY_ID=
AWS_SECRET_ACCESS_KEY=
AWS_DEFAULT_REGION=
