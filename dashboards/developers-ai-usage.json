{
  "version": 18,
  "variables": [],
  "tiles": {
    "0": {
      "type": "markdown",
      "title": "",
      "content": "# 🤖 Developers AI Usage\n\nVisão geral de uso de IA generativa pelos devs — custo, performance e adoção, agnóstico de ferramenta e modelo. Cada query abaixo é idêntica às validadas no notebook [copilot-chat-observability.md](../notebooks/copilot-chat-observability.md), só com o timeframe fixo em vez de herdar do dashboard, pra reduzir chance de erro de schema. Se algum tile ainda aparecer quebrado, copie a query do título e cole direto em **Add tile → Data → DQL** — ela é a mesma que já roda no Notebook."
    },

    "10": {
      "type": "markdown",
      "title": "",
      "content": "## 🩺 Sanity check + volume (24h)"
    },
    "11": {
      "type": "data",
      "title": "Sanity check — spans por serviço/agente",
      "query": "fetch spans, from:now()-1h\n| filter isNotNull(gen_ai.request.model)\n| summarize spans = count(), devs = countDistinct(user.email), by:{service.name, gen_ai.agent.name}\n| sort spans desc",
      "visualization": "table"
    },
    "12": {
      "type": "data",
      "title": "Volume — chats, tools, sessões",
      "query": "fetch spans, from:now()-24h\n| filter isNotNull(gen_ai.request.model)\n| summarize chats = countIf(startsWith(span.name, \"chat \")),\n            tools = countIf(startsWith(span.name, \"execute_tool \")),\n            sessions = countDistinct(gen_ai.conversation.id)",
      "visualization": "table"
    },

    "20": {
      "type": "markdown",
      "title": "",
      "content": "## 💰 FinOps — custo e tokens\n\n> Custo estimado via pricing hardcoded na DQL — ajuste conforme os provedores reais que sua empresa usa."
    },
    "21": {
      "type": "data",
      "title": "Custo total por modelo (24h)",
      "query": "fetch spans, from:now()-24h\n| filter isNotNull(gen_ai.usage.input_tokens)\n| fieldsAdd inp = toDouble(gen_ai.usage.input_tokens)\n| fieldsAdd out = toDouble(gen_ai.usage.output_tokens)\n| fieldsAdd cost_usd =\n    if(contains(gen_ai.request.model, \"opus\"),\n       (inp * 0.000015) + (out * 0.000075),\n    else: if(contains(gen_ai.request.model, \"mini\"),\n       (inp * 0.00000015) + (out * 0.0000006),\n    else: if(contains(gen_ai.request.model, \"claude\"),\n       (inp * 0.000003) + (out * 0.000015),\n    else: if(contains(gen_ai.request.model, \"gpt-4o\"),\n       (inp * 0.0000025) + (out * 0.00001),\n    else: (inp * 0.000002) + (out * 0.00001)))))\n| summarize total_cost = sum(cost_usd),\n            input_tokens = sum(inp),\n            output_tokens = sum(out),\n            requests = count(),\n            by:{gen_ai.request.model}\n| sort total_cost desc",
      "visualization": "table"
    },
    "22": {
      "type": "data",
      "title": "Custo por dev (top 20, 24h)",
      "query": "fetch spans, from:now()-24h\n| filter isNotNull(user.email) and isNotNull(gen_ai.usage.input_tokens)\n| fieldsAdd inp = toDouble(gen_ai.usage.input_tokens)\n| fieldsAdd out = toDouble(gen_ai.usage.output_tokens)\n| fieldsAdd cost_usd =\n    if(contains(gen_ai.request.model, \"opus\"),\n       (inp * 0.000015) + (out * 0.000075),\n    else: if(contains(gen_ai.request.model, \"mini\"),\n       (inp * 0.00000015) + (out * 0.0000006),\n    else: if(contains(gen_ai.request.model, \"claude\"),\n       (inp * 0.000003) + (out * 0.000015),\n    else: (inp * 0.0000025) + (out * 0.00001))))\n| summarize cost_usd = sum(cost_usd),\n            tokens = sum(inp + out),\n            chats = count(),\n            by:{user.email, user.name, user.team}\n| sort cost_usd desc\n| limit 20",
      "visualization": "table"
    },
    "23": {
      "type": "data",
      "title": "Custo por time (7 dias)",
      "query": "fetch spans, from:now()-7d\n| filter isNotNull(user.team) and isNotNull(gen_ai.usage.input_tokens)\n| fieldsAdd inp = toDouble(gen_ai.usage.input_tokens)\n| fieldsAdd out = toDouble(gen_ai.usage.output_tokens)\n| fieldsAdd cost_usd =\n    if(contains(gen_ai.request.model, \"opus\"),\n       (inp * 0.000015) + (out * 0.000075),\n    else: if(contains(gen_ai.request.model, \"mini\"),\n       (inp * 0.00000015) + (out * 0.0000006),\n    else: (inp * 0.0000025) + (out * 0.00001)))\n| summarize cost_usd = sum(cost_usd),\n            devs = countDistinct(user.email),\n            chats = count(),\n            by:{user.team}\n| sort cost_usd desc",
      "visualization": "table"
    },

    "30": {
      "type": "markdown",
      "title": "",
      "content": "## ⚙️ SRE — performance"
    },
    "31": {
      "type": "data",
      "title": "Latência P50/P95/P99 por modelo (24h)",
      "query": "fetch spans, from:now()-24h\n| filter isNotNull(gen_ai.request.model)\n| filter startsWith(span.name, \"chat \")\n| fieldsAdd dur_ms = duration / 1000000\n| summarize p50 = percentile(dur_ms, 50),\n            p95 = percentile(dur_ms, 95),\n            p99 = percentile(dur_ms, 99),\n            avg_ms = avg(dur_ms),\n            requests = count(),\n            by:{gen_ai.request.model}\n| sort p95 desc",
      "visualization": "table"
    },

    "40": {
      "type": "markdown",
      "title": "",
      "content": "## 🎯 Adoção — modelos, agentes, devs"
    },
    "41": {
      "type": "data",
      "title": "Modelos mais usados (7 dias)",
      "query": "fetch spans, from:now()-7d\n| filter isNotNull(gen_ai.request.model)\n| filter startsWith(span.name, \"chat \")\n| summarize chats = count(),\n            devs = countDistinct(user.email),\n            tokens = sum(toLong(gen_ai.usage.input_tokens) + toLong(gen_ai.usage.output_tokens)),\n            by:{gen_ai.request.model}\n| sort chats desc",
      "visualization": "table"
    },
    "42": {
      "type": "data",
      "title": "Agents / sub-agents ativos (24h)",
      "query": "fetch spans, from:now()-24h\n| filter isNotNull(gen_ai.agent.name)\n| summarize chats = count(),\n            tokens = sum(toLong(gen_ai.usage.input_tokens) + toLong(gen_ai.usage.output_tokens)),\n            by:{gen_ai.agent.name}\n| sort chats desc",
      "visualization": "table"
    },
    "43": {
      "type": "data",
      "title": "Top 10 devs por consumo (7 dias)",
      "query": "fetch spans, from:now()-7d\n| filter isNotNull(gen_ai.request.model)\n| filter startsWith(span.name, \"chat \")\n| filter isNotNull(user.email)\n| summarize chats = count(),\n            tokens = sum(toLong(gen_ai.usage.input_tokens) + toLong(gen_ai.usage.output_tokens)),\n            sessions = countDistinct(gen_ai.conversation.id),\n            by:{user.email, user.name, user.team}\n| sort chats desc\n| limit 10",
      "visualization": "table"
    },
    "44": {
      "type": "data",
      "title": "Sessões — top conversas por consumo (24h)",
      "query": "fetch spans, from:now()-24h\n| filter isNotNull(gen_ai.conversation.id)\n| summarize chats = count(),\n            total_tokens = sum(toLong(gen_ai.usage.input_tokens) + toLong(gen_ai.usage.output_tokens)),\n            by:{gen_ai.conversation.id, user.email}\n| sort total_tokens desc\n| limit 10",
      "visualization": "table"
    },

    "50": {
      "type": "markdown",
      "title": "",
      "content": "## 🧪 Evaluations (opcional — dt-evals)\n\nRequer [`dt-evals`](https://github.com/dynatrace-oss/dt-evals) rodando. Ver [docs/07-evaluations.md](../docs/07-evaluations.md)."
    },
    "51": {
      "type": "data",
      "title": "Pass rate por métrica (24h)",
      "query": "fetch bizevents, from:now()-24h\n| filter event.type == \"gen_ai.evaluation.result\"\n| summarize evaluations = count(),\n            pass_rate = countIf(gen_ai.evaluation.score.label == \"pass\") * 100.0 / count(),\n            avg_score = avg(gen_ai.evaluation.score.value),\n            by:{gen_ai.evaluation.name}\n| sort pass_rate asc",
      "visualization": "table"
    },

    "99": {
      "type": "markdown",
      "title": "",
      "content": "---\n### 📖 Como esses dados chegam aqui\n1. Ferramenta de IA do dev (Copilot Chat, Claude Code, Cursor, agente custom...) emite spans OTel GenAI\n2. Spans vão via OTLP HTTP pro OTel Collector\n3. Collector autentica e forwarda pro Dynatrace\n4. Este dashboard consulta via DQL — mesmas queries do [notebook](../notebooks/copilot-chat-observability.md)"
    }
  },
  "layouts": {
    "0":  { "x": 0,  "y": 0,  "w": 24, "h": 3 },
    "10": { "x": 0,  "y": 3,  "w": 24, "h": 1 },
    "11": { "x": 0,  "y": 4,  "w": 12, "h": 6 },
    "12": { "x": 12, "y": 4,  "w": 12, "h": 6 },
    "20": { "x": 0,  "y": 10, "w": 24, "h": 2 },
    "21": { "x": 0,  "y": 12, "w": 24, "h": 6 },
    "22": { "x": 0,  "y": 18, "w": 12, "h": 6 },
    "23": { "x": 12, "y": 18, "w": 12, "h": 6 },
    "30": { "x": 0,  "y": 24, "w": 24, "h": 2 },
    "31": { "x": 0,  "y": 26, "w": 24, "h": 6 },
    "40": { "x": 0,  "y": 32, "w": 24, "h": 2 },
    "41": { "x": 0,  "y": 34, "w": 12, "h": 6 },
    "42": { "x": 12, "y": 34, "w": 12, "h": 6 },
    "43": { "x": 0,  "y": 40, "w": 12, "h": 6 },
    "44": { "x": 12, "y": 40, "w": 12, "h": 6 },
    "50": { "x": 0,  "y": 46, "w": 24, "h": 2 },
    "51": { "x": 0,  "y": 48, "w": 24, "h": 6 },
    "99": { "x": 0,  "y": 54, "w": 24, "h": 3 }
  }
}
